name: password-manager
services:
  password-manager:
    build: .
    volumes:
      - ./password_manager.db:/password-manager/password_manager.db
      - ./default_credentials.txt:/password-manager/default_credentials.txt
    ports:
      - "443:443"